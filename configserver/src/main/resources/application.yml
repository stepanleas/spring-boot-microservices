spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # search-locations: "classpath:/config"
          search-locations: "file:///home//stepanleas//Desktop//Projects/spring-boot-microservices-course-config"

server:
  port: 8071